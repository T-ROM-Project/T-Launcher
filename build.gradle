
buildscript {
    repositories {
        // found an repo that supports old gradle etc. ...
        maven { url "https://archiva.interlocsolutions.com/archiva/repository/internal/" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.3'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
